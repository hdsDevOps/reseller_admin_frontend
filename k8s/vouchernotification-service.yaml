apiVersion: v1
kind: Service
metadata:
  name: adminfrontend-vouchernotification-service
spec:
  selector:
    app: adminfrontend-vouchernotification-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 4003
