apiVersion: v1
kind: Service
metadata:
  name: frontendadmin-vouchernotification-service
spec:
  selector:
    app: frontendadmin-vouchernotification-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 4003
