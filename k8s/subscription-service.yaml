apiVersion: v1
kind: Service
metadata:
  name: adminfrontend-subscription-service
spec:
  selector:
    app: adminfrontend-subscription-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 4004
