apiVersion: v1
kind: Service
metadata:
  name: adminfrontend-paymenthistory-service
spec:
  selector:
    app: adminfrontend-paymenthistory-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 4005
