apiVersion: v1
kind: Service
metadata:
  name: frontendadmin-paymenthistory-service
spec:
  selector:
    app: frontendadmin-paymenthistory-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 4005
